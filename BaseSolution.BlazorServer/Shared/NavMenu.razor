@using BaseSolution.BlazorServer.Data.Extensions
<MudNavMenu Color="Color.Primary">
    <MudNavLink Href="@RouteManager.User.List">Quản lý nhân viên</MudNavLink>
</MudNavMenu>
<MudNavMenu Color="Color.Primary">
    <MudNavLink Href="@RouteManager.Department.List">Quản lý rạp</MudNavLink>
</MudNavMenu>
<MudNavMenu Color="Color.Primary">
    <MudNavLink  Href="@RouteManager.FilmSchedule.List">Quản lý Lịch Chiếu</MudNavLink>
</MudNavMenu>
<MudNavMenu Color="Color.Primary">
    <MudNavLink Href="@RouteManager.Room.List">Quản lý phòng chiếu</MudNavLink>
</MudNavMenu>
<MudNavMenu Color="Color.Primary">
    <MudNavLink Href="@RouteManager.Customer.List">Quản lý khách hàng</MudNavLink>
</MudNavMenu>
<MudNavMenu Color="Color.Primary">
    <MudNavLink  Href="@RouteManager.Ticket.List">Quản lý vé</MudNavLink>
</MudNavMenu>
<MudNavMenu Color="Color.Primary">
    <MudNavLink  Href="@RouteManager.Bill.List">Quản lý hóa đơn</MudNavLink>
</MudNavMenu>
<MudNavMenu Color="Color.Primary">
    <MudNavLink  Href="@RouteManager.Film.List">Quản lý Phim</MudNavLink>
</MudNavMenu>
<MudNavMenu Color="Color.Primary">
    <MudNavLink  Href="@RouteManager.Seat.List">Quản lý Ghế</MudNavLink>
</MudNavMenu>
<MudNavMenu Color="Color.Primary">
    <MudNavLink Href="@RouteManager.Statistic.BillStatistic">Thống kê doanh thu</MudNavLink>
</MudNavMenu>
<MudNavMenu Color="Color.Primary">
    <MudNavLink Href="@RouteManager.Statistic.FilmStatistic">Thống kê bộ phim ăn khách</MudNavLink>
</MudNavMenu>
@* <MudNavMenu Color="Color.Primary">
    <MudNavLink Href="@RouteManager.RoomLayout.ExcelImport">Excel Import</MudNavLink>
</MudNavMenu> *@
<MudNavMenu Color="Color.Primary">
    <MudNavLink Href="@RouteManager.RoomLayout.List">Danh sách bố cục phòng chiếu</MudNavLink>
</MudNavMenu>

@code {
    private MudTheme _customTheme = new()
        {
            Palette = new Palette()
            {
                Primary = "#F97E0C"
            }
        };

    private async Task HandleNavLinkClick()
    {
        // Perform any necessary logic before navigation
        // Prevent the default behavior
        await Task.CompletedTask;
    }
}
